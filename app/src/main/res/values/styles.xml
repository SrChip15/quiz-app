<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ChoicesView">
        <item name="android:layout_marginLeft">@dimen/quiz_screen_left_border</item>
        <item name="android:layout_marginRight">@dimen/quiz_screen_right_border</item>
        <item name="android:paddingLeft">@dimen/radio_button_text_padding</item>
    </style>

    <style name="ChoicesText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:typeface">serif</item>
        <item name="android:textSize">@dimen/quiz_text_size</item>
    </style>

    <style name="ButtonText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:typeface">sans</item>
    </style>

    <style name="ResultsQuestionText" parent="TextAppearance.AppCompat.Display1">
        <item name="android:textSize">30sp</item>
    </style>

</resources>
